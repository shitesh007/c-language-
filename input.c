#include<stdio.h>

int main(){
    int a; 
    printf("enter the value of a:");
    scanf("%d", &a);//this function is is used for taking input from user 
    // %d = int %f= float   %c= char
    printf("The value of a is %d", a);
    return 0;
}