#include <stdio.h>

int main(){
    int a = 3;
    int b = 6;
    int c = 9;

    printf("The value is %d\n", a*b/c + 7);
    printf("The value is %d\n", 3*b/2*c + 7*a);
    printf("the value is %d \n ",a+b*c/2 +7);
    // the airhmetic function work in * / + - 
    return 0;
}